export default function Footer() {
  return (
    <footer className="footer-root">
      <p>
        © {new Date().getFullYear()} Zareth — portafolio desarrollado con React
      </p>
    </footer>
  )
}
